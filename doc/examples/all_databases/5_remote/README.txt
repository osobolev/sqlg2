Example of remote client-server connection.

This example uses port 8088 as port for HTTP server. If you have problems
using this port you can change it in remote.Server.HTTP_PORT field.

To run server in this example execute
    gradle runServer
command. To run client in this example execute
    gradle runClient
command.
